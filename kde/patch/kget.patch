# As long as Kget is Qt5 based we need to patch out the call
# for plasma-workspace:
#cat $CWD/patch/kget/kget.noworkspace.patch | patch -p1 --verbose || { touch ${SLACK_KDE_BUILD_DIR}/${PKGNAME}.failed ; continue ; }

