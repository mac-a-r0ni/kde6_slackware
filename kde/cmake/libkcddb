# Configure the Qt6 support first:
mkdir cmake-build-qt6
cd cmake-build-qt6
  cmake \
    "${KDE_BUILD_ARGS[@]}" \
    "${KDE_QT6_ARGS[@]}" \
    $KDE_OPT_ARGS \
    ..
cd -

# make can pick up from here.

